name: ProShield
main: com.snazzyatoms.proshield.ProShield
version: 1.2.5
api-version: 1.20
author: snazzyatoms
website: https://github.com/snazzyatoms/ProShield
description: >
  ProShield is a lightweight yet powerful land & plot protection plugin 
  for Spigot and Paper servers. Secure claims, manage trust & roles, 
  repel mobs, protect items, and provide intuitive GUIs for players and admins.

commands:
  proshield:
    description: Root ProShield command (help menu + admin tools dispatcher)
    usage: /<command> [help|reload|debug|bypass|admin|compass|info]
    aliases: [ps, shield]
    permission: proshield.use

  claim:
    description: Claim your current chunk or land
    usage: /<command>
    permission: proshield.use

  unclaim:
    description: Unclaim your current chunk or land
    usage: /<command>
    permission: proshield.use

  trust:
    description: Trust a player with optional role
    usage: /<command> <player> [role]
    permission: proshield.use
    aliases: [addtrust]

  untrust:
    description: Remove trust from a player
    usage: /<command> <player>
    permission: proshield.use
    aliases: [removetrust]

  roles:
    description: Open the trusted players / roles GUI
    usage: /<command>
    permission: proshield.use

  transfer:
    description: Transfer claim ownership to another player
    usage: /<command> <player>
    permission: proshield.use

permissions:
  # General Use
  proshield.use:
    description: Use ProShield commands & GUIs
    default: true

  proshield.flags:
    description: Allow players to toggle claim flags (in their own claims)
    default: true

  proshield.unlimited:
    description: Ignore max-claims limit
    default: op

  # Admin Core
  proshield.admin:
    description: Access admin tools (admin GUI, expansion requests, purge, force unclaim)
    default: op

  proshield.admin.reload:
    description: Reload ProShield config
    default: op
