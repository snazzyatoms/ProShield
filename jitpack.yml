jdk:
  - openjdk17

# Keep it simple: standard Maven build, skip tests by default
install:
  - mvn -B -DskipTests=true clean install

# (Optional) You can add env hints for memory if your project grows
# env:
#   MAVEN_OPTS: "-Xmx1g -XX:+UseG1GC"
