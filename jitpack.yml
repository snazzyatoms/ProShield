# JitPack build instructions for ProShield 1.0.7
jdk:
  - openjdk17

# Skip the default "mvn install" step and just package the plugin
install:
  - echo "Skipping default install step"

build:
  script:
    - mvn -B -DskipTests=true clean package
