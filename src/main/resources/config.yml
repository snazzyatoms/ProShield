# ProShield default configuration
# Storage format: chunks claimed per world.
# Each claim is stored under claims.<world:chunkX_chunkZ>
# Example created at runtime.

messages:
  prefix: "&3[ProShield]&r "

# Claims are stored/rewritten automatically by PlotManager.
claims: {}
