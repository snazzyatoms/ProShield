jdk:
  - openjdk21

before_install:
  - echo "Using OpenJDK 21 for build"

install:
  - mvn -B install -DskipTests

# Optional: specify Maven wrapper if you want reproducible builds
# build:
#   commands:
#     - ./mvnw -B clean package
