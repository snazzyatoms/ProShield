# ==================================================
# 🛡️ ProShield - JitPack Build Configuration
# Version: 1.2.5
# ==================================================

jdk:
  - openjdk17

before_install:
  - echo "Using JDK 17 for ProShield build"

install:
  - mvn -B install -DskipTests --file pom.xml

# Optional: publish artifacts for testing
cache:
  paths:
    - $HOME/.m2
