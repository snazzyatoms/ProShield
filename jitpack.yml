# path: jitpack.yml
jdk:
  - openjdk17

install:
  - mvn -B -V -DskipTests de.qaware.maven:go-offline-maven-plugin:resolve-dependencies

build:
  commands:
    - mvn -B -DskipTests clean package

artifacts:
  - path: target/*.jar
    name: ProShield
